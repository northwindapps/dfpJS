class NewtonRapson{
    constructor() {
      this.p = [-3.0, -3.0];
      this.grad = 0;
      this.func = 0;
      this.f=[0.0];
      //F(x)
      //F'(x)
    }


    //many want to know the value x for target y=0
    newtonRapson(target-y,guess-x,convergence){
        var x = guess-x;
        var iter = 0;
        var isLoop = true;
        while (isLoop) {
            // While True
            
            //     If (Abs(Y(x) - target) <= convergence) Then Exit Function
            //     NewtonRaphson = NewtonRaphson + 1
            //     x = x + (target - Y(x)) / dYdX(x)
            // Wend
            iter+=1;
        }


    }

}